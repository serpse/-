const a = +prompt('ввод цифры');
const c = +prompt('ввод цифры');
alert (`//Користувач ввів 2 і 2;
  ${a} + ${c} = ${a + c}
  ${a} - ${c} = ${a - c}
  ${a} * ${c} = ${a * c}
  ${a} / ${c} = ${a / c}`
  );